[project]
name = "simple-result"
version = "0.1.0"
description = "A simple Rust-like Result type for Python."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "coverage==7.11.3",
    "pytest==9.0.1",
    "ruff==0.14.5",
]
